export const sum = (v1, v2) => v1 + v2;
export const res = (v1, v2) => v1 - v2;
export const mult = (v1, v2) => v1 * v2;
export const div = (v1, v2) => (v2 === 0) ? "Operacion no valida" : v1 / v2;